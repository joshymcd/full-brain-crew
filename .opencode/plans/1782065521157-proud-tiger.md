# Plan: Move OpenCode Client Configuration to Root `.env`

## Goal

Load OpenCode connection settings from the repository root `.env` (one level above `webapp`) and use those values in both:

- The webapp OpenCode SDK client created in `webapp/src/main.tsx`.
- The Docker/OpenCode server startup command in `scripts/entrypoint.sh`.
- Local Docker Compose services for both the OpenCode server and the webapp.

## Requirements

- Add/use these env vars:
  - `OPENCODE_SERVER_URL` for the browser SDK base URL, e.g. `https://brain.joshmcd.xyz/`.
  - `OPENCODE_SERVER_USERNAME` for Basic Auth username.
  - `OPENCODE_SERVER_PASSWORD` for Basic Auth password.
  - `OPENCODE_PORT` for the host-side Docker port mapping.
  - `WEBAPP_PORT` for the host-side webapp port mapping.
- Configure Vite/Vite+ to load root `.env` from `../.env` relative to `webapp`.
- Allow `OPENCODE_*` variables to be exposed to the browser via Vite env handling.
- Keep OpenCode SDK client in TanStack Router context (`main.tsx`) and keep routes consuming it via context.

## Important Note

Because the OpenCode SDK client runs in the browser, `OPENCODE_SERVER_USERNAME` and `OPENCODE_SERVER_PASSWORD` will be exposed to the browser bundle/runtime if read through `import.meta.env`. This matches the requested approach, but it is not secret from end users.

## Files To Modify

### `webapp/package.json`

- Update the `dev` script to source the repository root `.env` before starting Vite+.
- Recommended script:
  - `set -a; [ -f ../.env ] && . ../.env; set +a; vp dev --port 3000`
- This keeps root `.env` as the single source of truth for local dev without requiring Vite `envDir`.

### `webapp/vite.config.ts`

- Add `envPrefix` to include `OPENCODE_`, while preserving Vite defaults:
  - `envPrefix: ["VITE_", "OPENCODE_"]`
- This is still needed because the OpenCode SDK client is created in browser code; Vite does not expose arbitrary env vars to `import.meta.env` unless they match an allowed prefix.
- Alternative if avoiding `envPrefix`: map root vars to `VITE_OPENCODE_*` in the dev script and use `import.meta.env.VITE_OPENCODE_*` in code. The recommended approach is `envPrefix` because Docker Compose already passes `OPENCODE_*` directly.

### `webapp/src/main.tsx`

- Remove hardcoded OpenCode URL and auth string.
- Read values from `import.meta.env`:
  - `import.meta.env.OPENCODE_SERVER_URL`
  - `import.meta.env.OPENCODE_SERVER_USERNAME`
  - `import.meta.env.OPENCODE_SERVER_PASSWORD`
- Build the Basic Auth header from the env values.
- Keep the router context shape unchanged:
  - `context: { opencodeClient: client }`
- Add safe defaults only where appropriate:
  - Username can default to `opencode`.
  - URL/password should be treated as required for real connectivity.

### `scripts/entrypoint.sh`

- Keep Docker/container runtime simple and consistent: OpenCode always listens on container port `8080`.
- Keep existing `PORT="${PORT:-8080}"` fallback if Railway/platform runtime needs it.
- Do not pass `OPENCODE_PORT` into the OpenCode command inside the container.
- Keep startup as:
  - `opencode web --port "${PORT}" --hostname 0.0.0.0`
- For local Docker Compose, set/keep `PORT=8080` inside the container if needed and map host port separately.

### `docker-compose.yml`

- Keep container ports hardcoded and simple:
  - OpenCode container port: `8080`
  - Webapp container port: `3000`
- Use env vars only at the host-to-container boundary:
  - OpenCode mapping: `"${OPENCODE_PORT:-8080}:8080"`
  - Webapp mapping: `"${WEBAPP_PORT:-3000}:3000"`
- Keep `env_file: .env` as-is.
- Add a separate `webapp` service for local development:
  - Build/run from `./webapp`.
  - Use the same root `.env` via `env_file: .env`.
  - Expose `${WEBAPP_PORT:-3000}:3000`.
  - Run Vite dev server bound to all interfaces: `vp dev --host 0.0.0.0 --port 3000`.
  - Mount `./webapp:/app` for local dev iteration and use an anonymous volume for `/app/node_modules` if needed.
- Add `depends_on: [app]` for convenience, so `docker compose up webapp` starts the OpenCode server too.

Cleaner Vite Docker dev recommendation:

- Use a lightweight Dockerfile in `webapp/Dockerfile.dev` rather than a long inline Compose command.
- It should:
  - Use `node:22-trixie-slim`.
  - Enable Corepack.
  - Set `WORKDIR /app`.
  - Copy package manifests if building dependencies into an image, or rely on the bind mount plus named `node_modules` volume for local dev.
  - Start `vp dev --host 0.0.0.0 --port 3000`.
- Compose remains readable and stable; the container always serves port `3000`.

Recommended local Compose shape:

```yaml
services:
  app:
    build: .
    ports:
      - "${OPENCODE_PORT:-8080}:8080"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./.vault:/vault

  webapp:
    working_dir: /app
    image: node:22-trixie-slim
    command: sh -lc "corepack enable && pnpm install && pnpm exec vp dev --host 0.0.0.0 --port 3000"
    ports:
      - "${WEBAPP_PORT:-3000}:3000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./webapp:/app
      - webapp_node_modules:/app/node_modules
    depends_on:
      - app

volumes:
  webapp_node_modules:
```

If `vp` is not available through `pnpm exec`, use the project script instead and update `webapp/package.json` dev script to bind to `0.0.0.0`, or install/run Vite+ through the documented Vite+ wrapper available in the repo.

### `.env.example`

- Add `OPENCODE_SERVER_URL=` near the OpenCode server vars.
- Add `OPENCODE_PORT=8080` near the server vars as the local host port mapping.
- Add `WEBAPP_PORT=3000` as the local host port mapping for Vite.
- Keep `OPENCODE_SERVER_USERNAME=opencode` and `OPENCODE_SERVER_PASSWORD=` documented.
- Update the note about `PORT` to clarify:
  - `PORT` can still be platform-injected inside deployed containers.
  - `OPENCODE_PORT` controls only the host-side Docker Compose mapping in local dev.

### `README.md`

- Update the env vars table to include:
  - `OPENCODE_SERVER_URL`
  - `OPENCODE_PORT`
  - `WEBAPP_PORT`
- Clarify that webapp browser code reads `OPENCODE_*` values via Vite.
- Clarify local Docker convention:
  - Container ports are fixed (`8080` for OpenCode, `3000` for webapp).
  - `.env` variables control only host-side mapped ports.

## Verification

1. Add values to root `.env`:
   - `OPENCODE_SERVER_URL=https://brain.joshmcd.xyz/`
   - `OPENCODE_SERVER_USERNAME=opencode`
   - `OPENCODE_SERVER_PASSWORD=...`
   - `OPENCODE_PORT=2703` or desired server port.
2. Run the webapp dev server from `webapp` with `vp run dev` (or `npm run dev`) and confirm it sources `../.env`.
3. Open `/temp/settings` and verify the OpenCode client connects using env-derived values.
4. Run `docker compose config` to verify the port mapping resolves correctly.
5. Run `docker compose up app webapp` and verify:
   - OpenCode starts inside Docker on `8080`.
   - Webapp starts inside Docker on `3000`.
   - Host mappings honor `OPENCODE_PORT` and `WEBAPP_PORT`.
   - `/temp/settings` connects to the configured `OPENCODE_SERVER_URL`.
6. Run targeted checks:
   - `vp lint` (expect existing unrelated repo issues may remain).
   - `npx tsc --noEmit` (expect existing `TS5101` deprecation unless fixed separately).
