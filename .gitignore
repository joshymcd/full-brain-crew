# Local env files — never commit real secrets. Keep the template.
.env
.env.*
!.env.example

# Local tooling / editor / OS cruft
.claude/
.DS_Store
Thumbs.db

# Local Docker Compose vault bind mount
.vault/

# Any stray exported Google credentials — never commit these
credentials.json
gws-credentials*.json
